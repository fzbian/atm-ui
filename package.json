{
  "name": "atm-ui",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-router-dom": "^7.8.2",
    "react-scripts": "5.0.1",
  "express": "^4.19.2",
  "sqlite3": "^5.1.7"
  },
  "scripts": {
  "start": "react-scripts start",
  "start:server": "node server/server.js",
  "dev": "concurrently -k \"npm:start\" \"npm:start:server\"",
  "start:prod": "npm run build && npm run start:server",
    "build": "react-scripts build",
    "test": "echo 'tests deshabilitados' && exit 0",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "proxy": "http://localhost:8081",
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
